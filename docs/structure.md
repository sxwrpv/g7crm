# Structure notes

This repo is split into four main areas:

- `web/` for the CRM frontend
- `database/` for SQL and schema changes
- `automations/n8n/` for workflow automation files
- `docs/` for project notes

Suggested next cleanup later:

- add deployment notes
- add environment setup notes
- add a simple changelog
